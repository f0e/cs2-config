// alias checkfwd ""
// alias checkback ""
// alias checkleft ""
// alias checkright ""

// alias +mfwd "-back; +forward; alias checkfwd +forward"
// alias +mback "-forward; +back; alias checkback +back"
// alias +mleft "-moveright; +left; alias checkleft +left"
// alias +mright "-left; +moveright; alias checkright +moveright"

// alias -mfwd "-forward; checkback; alias checkfwd"
// alias -mback "-back; checkfwd; alias checkback"
// alias -mleft "-left; checkright; alias checkleft"
// alias -mright "-moveright; checkleft; alias checkright"

// alias null "bind w +mfwd; bind s +mback; bind a +mleft; bind d +mright; echo nulling"
// alias notnull "bind w +forward; bind s +back; bind a +left; bind d +moveright; echo nonulls"
